import { Story, Preview, Props, Description, Meta} from '@storybook/addon-docs/blocks';
import Buttons from './index.vue';

import { DependenciesTable, Dependencies, Dependents } from 'storybook-addon-deps/blocks';
import { parameters } from '@storybook/vue';

<Meta title="Elements|Buttons" component={Buttons} parameters={{ component: Buttons }}/>

# Buttons

## Displays an image that represents a user or organization

Use an Buttons for attributing actions or content to specific users. The user's name should _always_ be present when using Buttons – either printed beside the avatar or in a tooltip.

<Preview withToolbar>
  <Story name="standard" height="100px">
    {{
    components : {Buttons},
    template: ' <Buttons/>',
  }}
  </Story>
</Preview>

<Props of={Buttons} />

# NPM package 
```javascript
 npm install --save @xcompany-design-system/button
```

# General usage code

```javascript
import ButtonCmpnt from '@xcompany-design-system/button';

<ButtonCmpnt/>
```

## Table dependents + dependencies
<DependenciesTable titleDependencies='Dependencies' titleDependents='Dependents' of={Buttons} />



# Variations

### this can have four varients

see the below usage formats

#### Regular
<Story name="sizes" height="100px">
  {{
    components : {Buttons},
    template: ' <Buttons/>',
  }}
</Story>





